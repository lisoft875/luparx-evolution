{
  "name": "@luparx/platform",
  "private": true,
  "version": "0.1.0",
  "description": "LupaRX platform back-office \u2014 the product operator's console: municipalities, global user registry, global audit log, catalogs and reports (React + Vite web app, no self-registration, MFA-only).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "preview:demo": "vite build --config vite.preview.config.ts"
  },
  "dependencies": {
    "@luparx/api-client": "*",
    "@luparx/auth": "*",
    "@luparx/features": "*",
    "@luparx/i18n": "*",
    "@luparx/ui": "*",
    "@tanstack/react-query": "^5.62.7",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "react-router-dom": "^6.28.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.7.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.21"
  }
}
